* Convert all JavaScript files to TypeScript, update var to let or const, and adjust types. Retain original directories and change extensions.
* Resolve dependencies to support typescript.
* Initialize TypeScript with command `npx tsc --init`.
* Find all build Scripts of webpack, Gulp, Vite and etc. Update those build scripts to support typescript.
* Find all lint configuration such as ESLint and Stylelint and etc. Update those configuration to support typescript.